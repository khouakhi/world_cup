rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All reads/writes go through Next.js API routes (Firebase Admin SDK).
    // Client apps cannot access Firestore directly.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
